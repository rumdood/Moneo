version: "3.8"
services:
  poller:
    image: moneo-poller
    volumes:
      - ./appsettings.json:/App/appsettings.json
  moneo.taskmanagment.api:
    image: moneo.taskmanagment.api
    build:
      context: .
      dockerfile: src/Moneo.Hosts/Moneo.TaskManagment.Api/Dockerfile
